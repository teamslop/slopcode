name: "Setup Git Committer"
description: "Create app token and configure git user"
inputs:
  slopcode-app-id:
    description: "SlopCode GitHub App ID"
    required: false
  slopcode-app-secret:
    description: "SlopCode GitHub App private key"
    required: false
outputs:
  token:
    description: "GitHub App token"
    value: ${{ steps.apptoken.outputs.token || github.token }}
  app-slug:
    description: "GitHub App slug"
    value: ${{ steps.apptoken.outputs.app-slug || 'github-actions' }}
runs:
  using: "composite"
  steps:
    - name: Create app token
      id: apptoken
      if: ${{ inputs.slopcode-app-id != '' && inputs.slopcode-app-secret != '' }}
      uses: actions/create-github-app-token@v2
      with:
        app-id: ${{ inputs.slopcode-app-id }}
        private-key: ${{ inputs.slopcode-app-secret }}
        owner: ${{ github.repository_owner }}

    - name: Configure git user
      run: |
        slug="${{ steps.apptoken.outputs.app-slug || 'github-actions' }}"
        git config --global user.name "${slug}[bot]"
        git config --global user.email "${slug}[bot]@users.noreply.github.com"
      shell: bash

    - name: Clear checkout auth
      run: |
        git config --local --unset-all http.https://github.com/.extraheader || true
      shell: bash

    - name: Configure git remote
      run: |
        git remote set-url origin https://x-access-token:${{ steps.apptoken.outputs.token || github.token }}@github.com/${{ github.repository }}
      shell: bash
